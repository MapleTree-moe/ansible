# {{ ansible_managed }}
# {{ ansible_fqdn }}
# TODO: it would be nice to have the recent git commit here

[Socket]
# forward 443 to 8443 using firewalld
ListenStream=0.0.0.0:8443
FileDescriptorName=websecure
Service=traefik.service

[Install]
WantedBy=sockets.target
