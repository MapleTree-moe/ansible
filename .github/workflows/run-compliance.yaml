###
### mapletree.moe - Automation
### Run compliance playbook on github actions
###
---
name: Run ansible compliance policy playbook

# yamllint disable-line rule:truthy
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'play/run-compliance.yaml'
      - 'tasks/compliance_*.yaml'

env:
  HCLOUD_TOKEN: ${{secrets.HCLOUD_TOKEN}}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Install Ansible
        shell: bash
        run: |
          sudo apt update
          sudo apt install -y ansible
      - name: Run Ansible Playbook - run-compliance.yaml
        uses: dawidd6/action-ansible-playbook@v3
        with:
          playbook: plays/run-compliance.yaml
          key: ${{secrets.SSH_PRIVATE_KEY}}
          vault_password: ${{secrets.VAULT_PASSWORD}}
          requirements: requirements.yaml
